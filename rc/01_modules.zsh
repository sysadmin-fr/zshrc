# -*- shell-script -*-
# Load globaly used modules

# Ensure zutil is loaded (zstyle)
zmodload zsh/zutil

# Define "stat"/"zstat" function:
zmodload zsh/stat

# Define strftime, EPOCHSECONDS
zmodload zsh/datetime

return
